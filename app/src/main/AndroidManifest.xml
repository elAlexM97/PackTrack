<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paquete.produccion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Produccion">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".LoginSelectionActivity" />
        <activity android:name=".MaterialsLoginActivity" />
        <activity android:name=".ProductionLoginActivity" />
        <activity android:name=".BaseDashboardActivity" />
        <activity android:name=".MaterialsDashboardActivity" />
        <activity android:name=".ProductionDashboardActivity" />
        <activity android:name=".QRScannerActivity"
            android:screenOrientation="portrait" />
        
        <!-- Permisos para cámara (QR Scanner) -->
        <uses-feature android:name="android.hardware.camera" android:required="false" />
        <uses-permission android:name="android.permission.CAMERA" />
    </application>

</manifest>